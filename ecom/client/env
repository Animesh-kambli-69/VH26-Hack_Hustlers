# ------------------------------------------------------------------
# ShopVerse server environment
# Existing real environment variables always override these values.
# ------------------------------------------------------------------

# --- API server ---
NODE_ENV=development
HOST=localhost
PORT=3001

# ------------------------------------------------------------------
# Phase 2 (caching & scaling) — placeholders, not wired yet.
# Uncomment when the cache/DB layers are implemented.
# ------------------------------------------------------------------

# Redis cache (will be read by server/src/cache/ when added)
# REDIS_URL=redis://127.0.0.1:6379

# PostgreSQL (will replace the demo JSON file store in db/fileDb.js)
# DATABASE_URL=postgres://shopverse:shopverse@127.0.0.1:5432/shopverse
